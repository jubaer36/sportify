# .env.example — template to commit
# Copy this to .env and replace values locally.

# Database (required)
DB_URL=jdbc:postgresql://localhost:5432/sportify
DB_USERNAME="Your Database Username"
DB_PASSWORD="Your Database Password"

# Optional: server port override
SERVER_PORT="Your Server Port"

# Optional JWT keys (example placeholders)
PROVISORR_JWT_SECRET=replace-this-with-your-secret
PROVISORR_JWT_EXPIRATION_MS=86400000
PROVISORR_JWT_REFRESH_EXPIRATION_MS=604800000
